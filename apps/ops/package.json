{
  "name": "@viva/ops",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "exports": "./dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@viva/ingestion-tools": "1.0.0",
    "fastify": "^5.6.0"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=24"
  }
}
